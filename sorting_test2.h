#ifndef SORTING_TEST2_H
#define SORTING_TEST2_H

#include <stddef.h>

void sorting_test2(unsigned* (randomizer)(size_t), void (algorithm)(unsigned*, size_t), size_t *N, size_t size_N, const char *filename);

#endif // #ifndef SORTING_TEST2_H